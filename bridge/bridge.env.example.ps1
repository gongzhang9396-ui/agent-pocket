# 复制为 bridge.env.ps1；不要把真实凭据提交到 Git。
$env:AGENT_POCKET_PROJECT_ROOTS = "C:\Projects"
$env:AGENT_POCKET_WSS_URL = "wss://agent.example.com/your-high-entropy-path"
# Firebase 配置提供后再启用：
# $env:AGENT_POCKET_FIREBASE_SERVICE_ACCOUNT = "C:\secure\agent-pocket-firebase.json"
