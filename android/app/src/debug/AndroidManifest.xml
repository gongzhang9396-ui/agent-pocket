<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Debug-only, deterministic UI preview; absent from release builds. -->
        <activity android:name=".UiPreviewActivity" android:exported="true" />
    </application>
</manifest>
